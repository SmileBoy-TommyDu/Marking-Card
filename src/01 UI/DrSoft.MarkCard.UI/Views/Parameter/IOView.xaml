<UserControl x:Class="DrSoft.MarkCard.UI.Views.IOView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DrSoft.MarkCard.UI.Views" 
             xmlns:uc="clr-namespace:DrSoft.MarkCard.CommonUI.UserControls;assembly=DrSoft.MarkCard.CommonUI"
             mc:Ignorable="d" 
             Background="#F5F5F5"
             Foreground="#2A2C2B"
             d:DesignHeight="420" 
             d:DesignWidth="328">

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <ScrollViewer VerticalScrollBarVisibility="Auto">

            <Grid Margin="8,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="4"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Border Grid.Row="1" Style="{StaticResource IOBoard}">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="3" Color="#000000" BlurRadius="10" Direction="315" Opacity="0.15"/>
                    </Border.Effect>
                    <StackPanel>
                        <!-- 输入 IO -->
                        <TextBlock Text="输入"  Style="{StaticResource MainHeaderLabel}"/>
                        <ItemsControl    ItemsSource="{Binding InputLED}" >
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Rows="2" Columns="8" HorizontalAlignment="Stretch"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="4" HorizontalAlignment="Center"
                                            ToolTip="{Binding Description}"
                                            MouseEnter="OnLedMouseEnter"
                                            MouseLeave="OnLedMouseLeave">
                                        <!-- 指示灯圆形 -->
                                        <Image Width="16" Height="16" Margin="0,0,0,4">
                                            <Image.Style>
                                                <Style TargetType="Image">
                                                    <Setter Property="Source" Value="/Resource/image/light_off.png"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsLit}" Value="True">
                                                            <Setter Property="Source" Value="/Resource/image/light_on.png"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Image.Style>
                                        </Image>
                                        <!-- 序号 -->
                                        <TextBlock Text="{Binding Index}" HorizontalAlignment="Center" FontSize="12" FontWeight="Medium"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                </Border>




                <Border Grid.Row="2" Style="{StaticResource IOBoard}">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="3" Color="#000000" BlurRadius="10" Direction="315" Opacity="0.15"/>
                    </Border.Effect>
                    <StackPanel>
                        <!-- 输出 IO -->
                        <TextBlock Text="输出"  Style="{StaticResource MainHeaderLabel}"/>
                        <ItemsControl  ItemsSource="{Binding  OutputLED}" >
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Rows="2" Columns="8" HorizontalAlignment="Stretch"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>

                                    <StackPanel Margin="4" HorizontalAlignment="Center"
                                            ToolTip="{Binding Description}"
                                            MouseEnter="OnLedMouseEnter"
                                            MouseLeave="OnLedMouseLeave">
                                        <Button Command="{Binding DataContext.SetOutputCommand,   RelativeSource={RelativeSource AncestorType=ItemsControl}}"   CommandParameter="{Binding Index}">

                                            <Button.Content>
                                                <StackPanel>
                                                    <!-- 指示灯圆形 -->
                                                    <Image Width="16" Height="16" Margin="0,0,0,4">
                                                        <Image.Style>
                                                            <Style TargetType="Image">
                                                                <Setter Property="Source" Value="/Resource/image/light_off.png"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsLit}" Value="True">
                                                                        <Setter Property="Source" Value="/Resource/image/light_on.png"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Image.Style>
                                                    </Image>
                                                    <!-- 序号 -->
                                                    <TextBlock Text="{Binding Index}" HorizontalAlignment="Center" FontSize="12" FontWeight="Medium"/>
                                                </StackPanel>
                                            </Button.Content>
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Background" Value="Gold"/>
                                                    <Setter Property="BorderThickness" Value="0"/>
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <ContentPresenter Content="{TemplateBinding Content}"/>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Opacity" Value="0.85"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>

                                    </StackPanel>

                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                </Border>
                <Border Grid.Row="3"  Style="{StaticResource IOBoardGray}" Height="58">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="3" Color="#000000" BlurRadius="10" Direction="315" Opacity="0.15"/>
                    </Border.Effect>
                    <Grid  >
                        <!-- 内阴影层：模拟 inset box-shadow (参考 IconButton) -->
                        <Border x:Name="ShadowLayer"    CornerRadius="6"    Opacity="1"    IsHitTestVisible="False">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,0.6">
                                    <GradientStop Color="#33000000" Offset="0"/>
                                    <GradientStop Color="#00000000" Offset="0.08"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                        <Border x:Name="ShadowLayer1"   CornerRadius="6"   Opacity="1"    IsHitTestVisible="False">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0.1,0">
                                    <GradientStop Color="#33000000" Offset="0"/>
                                    <GradientStop Color="#00000000" Offset="0.08"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                        <TextBlock Text="说明:" Margin="8" />
                        <TextBlock x:Name="IOText" Margin="40,8,8,8" Text="{Binding IODescription}" />
                    </Grid>
                </Border>
            </Grid>

        </ScrollViewer>
        <!--<Button Margin="8,0" HorizontalAlignment="Right" Grid.Row="1"  Content="套用当前页面"  Style="{StaticResource  SecondActionButtonStyle}" Command="{Binding ApplyCommand}"/>-->

    </Grid>

</UserControl>

<UserControl x:Class="DrSoft.MarkCard.UI.Views.Shape.TextParamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"   
             xmlns:uc="clr-namespace:DrSoft.MarkCard.CommonUI.UserControls;assembly=DrSoft.MarkCard.CommonUI"  
             mc:Ignorable="d"           
            Background="#F5F5F5"
                d:DesignHeight="420"
   d:DesignWidth="328"
           >
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <ScrollViewer MinHeight="300"  VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="8">
                <TextBlock Text="中文字型" Style="{StaticResource HeaderLabel}" />
                <StackPanel Margin="0,4" Orientation="Horizontal">
                    <ComboBox ItemsSource="{Binding FontFamilyList}" SelectedItem="{Binding ShareTextModel.CurrentFontFamily}" Width="120" Style="{StaticResource DrComboxStyle}"/>
                    <!--<ComboBox Margin="8,0,0,0" ItemsSource="{Binding HorizontalAlignList}" SelectedIndex="{Binding ShareTextModel.TextAlign}" Width="88" Style="{StaticResource DrComboxStyle}"/>-->
                    <CheckBox Content="斜体" IsChecked="{Binding ShareTextModel.IsItalic}" Margin="8,0,4,0" Style="{StaticResource DrCheckBoxStyle}"/>
                    <CheckBox Content="粗体" IsChecked="{Binding ShareTextModel.IsBold}" Margin="8,0,4,0" Style="{StaticResource DrCheckBoxStyle}"/>
                </StackPanel>

                <Grid  >

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="字号"    Style="{StaticResource HeaderLabel}"  />
                <TextBlock Margin="8,0,0,0" Grid.Row="0" Grid.Column="1" Text="字距" Style="{StaticResource HeaderLabel}" />
                <uc:NumericUpDownControl Grid.Row="1" Grid.Column="0" Title="" Unit="" Value="{Binding ShareTextModel.FontSize}" Style="{StaticResource MainNumericUpDown}" />
                <uc:NumericUpDownControl Margin="8,0,0,0" Grid.Row="1" Grid.Column="1" Title="" Unit="" Value="{Binding ShareTextModel.CharSpacing}" Style="{StaticResource MainNumericUpDown}"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="行高" Style="{StaticResource HeaderLabel}" />
                <uc:NumericUpDownControl Grid.Row="3" Grid.Column="0" Title="" Unit="" Value="{Binding ShareTextModel.LineHeight}" Style="{StaticResource MainNumericUpDown}"/>

                </Grid>

                <StackPanel Margin="2">
                    <TextBlock Text="文本内容" Style="{StaticResource HeaderLabel}" />

                    <Border Grid.Row="3" Background="White"  Style="{StaticResource IOBoardGray}" >
                        <Border.Effect>
                            <DropShadowEffect ShadowDepth="1" Color="#000000" BlurRadius="10" Direction="315" Opacity="0.1"/>
                        </Border.Effect>
                        <Grid  >
                            <!-- 内阴影层：模拟 inset box-shadow (参考 IconButton) -->
                            <Border x:Name="ShadowLayer"    CornerRadius="4"    Opacity="1"    IsHitTestVisible="False">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,0.6">
                                        <GradientStop Color="#33000000" Offset="0"/>
                                        <GradientStop Color="#00000000" Offset="0.08"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>
                            <Border x:Name="ShadowLayer1"   CornerRadius="4"   Opacity="1"    IsHitTestVisible="False">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0.1,0">
                                        <GradientStop Color="#33000000" Offset="0"/>
                                        <GradientStop Color="#00000000" Offset="0.08"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>
                            <TextBox Text="{Binding ShareTextModel.Text}" BorderThickness="0"  Margin="6"
Background="White"
Height="100"
TextAlignment="Left"
HorizontalContentAlignment="Left"
AcceptsReturn="True"
TextWrapping="Wrap"
VerticalScrollBarVisibility="Auto" />
                        </Grid>
                    </Border>

                </StackPanel>


            </StackPanel>
        </ScrollViewer>
        <Border Grid.Row="1" BorderBrush="#E2E2E2" BorderThickness="0,1,0,0" HorizontalAlignment="Stretch"/>
        <Button  HorizontalAlignment="Right" Grid.Row="1"   Content="套用当前页面" Style="{StaticResource  ActionButtonStyle}" Width="116"  Command="{Binding ApplyCommand}"/>
    </Grid>
</UserControl>

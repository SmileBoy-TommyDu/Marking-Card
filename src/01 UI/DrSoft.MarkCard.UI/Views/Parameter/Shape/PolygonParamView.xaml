<UserControl x:Class="DrSoft.MarkCard.UI.Views.Shape.PolygonParamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:uc="clr-namespace:DrSoft.MarkCard.CommonUI.UserControls;assembly=DrSoft.MarkCard.CommonUI"
             xmlns:conv="clr-namespace:DrSoft.MarkCard.UI.Converters"
             Background="#F5F5F5"
             mc:Ignorable="d">
    <UserControl.Resources>
        <conv:NumericGreaterOrEqualConverter x:Key="NumericGE" />
    </UserControl.Resources>

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <ScrollViewer MinHeight="300"  VerticalScrollBarVisibility="Auto">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal">
                    <TextBlock  Text="顶点数" Style="{StaticResource LabelText}"/>
                    <uc:NumericUpDownControl x:Name="PointNumber" MinValue="3" Step="1" DecimalPlaces="0" Value="{Binding SideCount}" Style="{StaticResource  MainNumericUpDown}"/>

                </StackPanel>

                <StackPanel  Grid.Row="1"  Orientation="Horizontal" Margin="0,8,0,0" Cursor="">
                    <uc:IconButton Background="White" x:Name="regular" Width="72" Height="72" IconHeight="60" IconWidth="60" Margin="6" GroupName="PolygonSubType" Style="{StaticResource LargeIconButton}" IconImage="pack://application:,,,/Resource/image/star0.png" IsChecked="{Binding SubType, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=Regular}"  />
                    <uc:IconButton Background="White" x:Name="star" Width="72" Height="72" IconHeight="60" IconWidth="60" Margin="6" GroupName="PolygonSubType" Style="{StaticResource LargeIconButton}" IconImage="pack://application:,,,/Resource/image/star1.png" IsChecked="{Binding SubType, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=Star}" IsEnabled="{Binding ElementName=PointNumber, Path=Value, Converter={StaticResource NumericGE}, ConverterParameter=3}"  />
                </StackPanel>
            </Grid>
        </ScrollViewer>
        <Border Grid.Row="1" BorderBrush="#E2E2E2" BorderThickness="0,1,0,0" HorizontalAlignment="Stretch"/>
        <Button HorizontalAlignment="Right" Grid.Row="1"  Content="套用当前页面"   Style="{StaticResource  ActionButtonStyle}" Width="116"  Command="{Binding ApplyCommand}"/>

    </Grid>
</UserControl>

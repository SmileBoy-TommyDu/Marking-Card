<UserControl x:Class="DrSoft.MarkCard.UI.Views.Shape.RectangleParamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:uc="clr-namespace:DrSoft.MarkCard.CommonUI.UserControls;assembly=DrSoft.MarkCard.CommonUI"  
             mc:Ignorable="d"  
             Background="#F5F5F5"
            >

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <ScrollViewer MinHeight="300"  VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="8">
                <StackPanel  Margin="0,4" HorizontalAlignment="Left" VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Text="单位" Style="{StaticResource LabelText}"/>
                    <ComboBox Width="120"  SelectedValue="{Binding UnitModel}" SelectedValuePath="Content" Style="{StaticResource DrComboxStyle}">
                        <ComboBoxItem Content="%"/>
                        <ComboBoxItem Content="mm"/>
                    </ComboBox>
                    <RadioButton Content="圆角"  IsChecked="{Binding IsRound}" Margin="11,4" Style="{StaticResource DrRadioButtonStyle}"/>
                    <RadioButton Content="倒角" IsChecked="{Binding IsChamfer}"  Margin="4"  Style="{StaticResource DrRadioButtonStyle}"/>

                </StackPanel>

                <CheckBox Content="四角统一" IsChecked="{Binding IsAllSame}"  Margin="0,4" Style="{StaticResource DrCheckBoxStyle}"/>

                <Grid   >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>


                    <uc:NumericUpDownControl Grid.Row="0" Grid.Column="0" Value="{Binding  Topleft}" Unit=""  Title="" Style="{StaticResource MainNumericUpDown}"/>
                    <uc:NumericUpDownControl Grid.Row="0" Grid.Column="1" Margin="8,4" Value="{Binding  Model.TopRight}" Unit=""  Title="" Style="{StaticResource MainNumericUpDown}"  IsEnabled="{Binding IsAllSame, Converter={StaticResource InverseBoolConverter}}"/>
                    <uc:NumericUpDownControl Grid.Row="1" Grid.Column="0" Value="{Binding  Model.BottomLeft}" Unit=""  Title="" Style="{StaticResource MainNumericUpDown}"  IsEnabled="{Binding IsAllSame, Converter={StaticResource InverseBoolConverter}}"/>
                    <uc:NumericUpDownControl Grid.Row="1" Grid.Column="1" Margin="8,4" Value="{Binding  Model.BottomRight}" Unit=""  Title="" Style="{StaticResource MainNumericUpDown}"  IsEnabled="{Binding IsAllSame, Converter={StaticResource InverseBoolConverter}}"/>
                </Grid>

            </StackPanel>
        </ScrollViewer>
        <Border Grid.Row="1" BorderBrush="#E2E2E2" BorderThickness="0,1,0,0" HorizontalAlignment="Stretch"/>
        <Button  HorizontalAlignment="Right" Grid.Row="1"   Content="套用当前页面" Style="{StaticResource  ActionButtonStyle}" Width="116"  Command="{Binding ApplyCommand}"/>


    </Grid>
</UserControl>

<UserControl x:Class="DrSoft.MarkCard.UI.Views.EngravingParamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DrSoft.MarkCard.UI.Views"             
             xmlns:conv="clr-namespace:DrSoft.MarkCard.UI.Converters"  
             xmlns:uc="clr-namespace:DrSoft.MarkCard.CommonUI.UserControls;assembly=DrSoft.MarkCard.CommonUI"
             mc:Ignorable="d" 
             Background="#F5F5F5" 
                 d:DesignHeight="420"
    d:DesignWidth="328">
    <UserControl.Resources>
        <!--实例化转换器-->
        <conv:BoolToBrushConverter x:Key="BoolToBrushConverter" 
                               TrueBrush="Black" 
                               FalseBrush="#A0A0A0"/>

        <Style x:Key="ucNumeric" TargetType="uc:NumericUpDownControl">
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Margin" Value="4,0,0,0"/>
        </Style>

    </UserControl.Resources>
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <ScrollViewer MinHeight="300"  VerticalScrollBarVisibility="Auto">
            <Grid Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <!-- 底部按钮组 -->
            </Grid.RowDefinitions>

            <!-- 1. 参数 -->
            <Grid  Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="*" />

                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>

                </Grid.RowDefinitions>

                <TextBlock  Grid.Row="0" Text="基础参数" Style="{StaticResource HeaderLabel}"/>

                <TextBlock Margin="0,0,0,0" HorizontalAlignment="Right" Grid.Row="1" Text="雕刻次数" VerticalAlignment="Center"/>
                <uc:NumericUpDownControl  MinValue="0" Margin="4,0,0,0"  Grid.Row="1" Grid.Column="1"  DecimalPlaces="0" Title="" Unit="次" Value="{Binding Model.EngraveCount}"   Style="{StaticResource MainNumericUpDown}" />

                <TextBlock Margin="0,4,0,0" HorizontalAlignment="Right" Grid.Row="2" Text="点雕刻时间" VerticalAlignment="Center" />
                <uc:NumericUpDownControl  MinValue="0" Margin="4,4,0,0"  Grid.Row="2" Grid.Column="1" Title="" Unit="ms" Value="{Binding Model.DotEngraveTime}"   Style="{StaticResource MainNumericUpDown}"/>
            </Grid>


            <!-- 参数输入 -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="*"/>
      
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="雕刻参数" Style="{StaticResource HeaderLabel}"/>

                <StackPanel Visibility="Collapsed"  Grid.Row="1" Orientation="Horizontal" Grid.ColumnSpan="2">
                    <CheckBox Content="外框" IsChecked="{Binding Model.IsOutline}"  Style="{StaticResource DrCheckBoxStyle}"/>
                    <CheckBox Content="填满" IsChecked="{Binding Model.IsFill}" Style="{StaticResource DrCheckBoxStyle}"/>
                    <CheckBox Content="填满优先" IsChecked="{Binding Model.IsFillPriority}" Style="{StaticResource DrCheckBoxStyle}"/>
                </StackPanel>


                <TextBlock Margin="0,0,0,0" HorizontalAlignment="Right" Grid.Row="1" Text="雕刻速度" VerticalAlignment="Center"/>
                <uc:NumericUpDownControl  MinValue="0" Margin="4,0,0,0"  Grid.Row="1" Grid.Column="1"  Title="" Unit="mm/ms" Value="{Binding Model.Speed}"   Style="{StaticResource MainNumericUpDown}"/>


                <TextBlock Margin="0,4,0,0"  HorizontalAlignment="Right" Grid.Row="2" Text="激光功率" VerticalAlignment="Center"/>
                <uc:NumericUpDownControl  MinValue="0" Margin="4,4,0,0"   Grid.Row="2" Grid.Column="1"  Title="" Unit="%" Value="{Binding Model.Power}"   Style="{StaticResource MainNumericUpDown}"/>


                <TextBlock Margin="0,4,0,0"  HorizontalAlignment="Right" Grid.Row="3" Text="激光频率" VerticalAlignment="Center"/>
                <uc:NumericUpDownControl  MinValue="0" Margin="4,4,0,0"  Grid.Row="3" Grid.Column="1"  Title="" Unit="kHz" Value="{Binding Model.Frequency}"   Style="{StaticResource MainNumericUpDown}"/>
   
            </Grid>

        </Grid>
            </ScrollViewer>
        <Border Grid.Row="1" BorderBrush="#E2E2E2" BorderThickness="0,1,0,0" HorizontalAlignment="Stretch"/>
        <Button  HorizontalAlignment="Right" Grid.Row="1" Content="套用当前页面" Style="{StaticResource  ActionButtonStyle}" Width="116"  Command="{Binding ApplyCommand}" />

    </Grid>
</UserControl>

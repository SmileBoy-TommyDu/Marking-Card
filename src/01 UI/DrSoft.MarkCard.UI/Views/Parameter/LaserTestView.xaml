<UserControl x:Class="DrSoft.MarkCard.UI.Views.LaserTestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DrSoft.MarkCard.UI.Views"  
             xmlns:uc="clr-namespace:DrSoft.MarkCard.CommonUI.UserControls;assembly=DrSoft.MarkCard.CommonUI"
             mc:Ignorable="d"  
             Background="#F5F5F5" 
             Foreground="#2A2C2B"
             d:DesignHeight="420"
             d:DesignWidth="328">
 

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <ScrollViewer  Grid.Row="0"  VerticalScrollBarVisibility="Auto">


            <!-- 主内容区域 -->
            <StackPanel Margin="8" >

                <!-- 参数设置区 -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="32"/>
                        <RowDefinition Height="32"/>
                        <RowDefinition Height="32"/>
                    </Grid.RowDefinitions>

                    <!-- 功率 -->
                    <TextBlock Grid.Row="0" Text="功率" VerticalAlignment="Center"  Style="{StaticResource LabelText}"/>

                    <uc:NumericUpDownControl  Unit="%" MinValue="0" Grid.Row="0" Grid.Column="1" Value="{Binding Power}"   Style="{StaticResource MainNumericUpDown}"/>

                    <!-- 频率 -->
                    <TextBlock Grid.Row="1" Text="频率" VerticalAlignment="Center" Style="{StaticResource LabelText}"/>
                    <uc:NumericUpDownControl  Unit="kHz" MinValue="0" Grid.Row="1" Grid.Column="1" Value="{Binding Frequency}"    Style="{StaticResource MainNumericUpDown}"/>
                    <!-- 时间 -->
                    <TextBlock Grid.Row="2" Text="持续时间" VerticalAlignment="Center" Style="{StaticResource LabelText}"/>

                    <uc:NumericUpDownControl  Unit="s" MinValue="0" Grid.Row="2" Grid.Column="1" Value="{Binding Time}"    Style="{StaticResource MainNumericUpDown}"/>

                </Grid>

                <!-- 开激光控制区 -->
                <!-- 开激光模式一 -->
                <TextBlock Text="开激光模式一" Style="{StaticResource HeaderLabel}"/>
                <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                    <Button Content="手动开激光"   Style="{StaticResource ActionButtonStyle}" Width="88" HorizontalAlignment="Left" Margin="4" Command="{Binding ManualLaserCommand}"/>

                    <Button Content="手动关激光"   Style="{StaticResource SecondActionButtonStyle}" Width="88" HorizontalAlignment="Left" Margin="4" Command="{Binding ManualCloseLaserCommand}"/>
                </StackPanel>

                <!-- 开激光模式二 -->
                <TextBlock Text="开激光模式二" Style="{StaticResource HeaderLabel}"/>

                <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                    <ComboBox Margin="5" Width="120" Style="{StaticResource DrComboxStyle}" ItemsSource="{Binding InputLEDDescrpition}" SelectedIndex="{Binding SelectInputID}"/>
                    <CheckBox Content="外部输入" Margin="5" Style="{StaticResource DrCheckBoxStyle}" IsChecked="{Binding IsOutControl}"/>

                </StackPanel>





            </StackPanel>
        </ScrollViewer>
        <!--<Button Margin="8,0" HorizontalAlignment="Right" Grid.Row="1"  Content="套用当前页面"   Style="{StaticResource  SecondActionButtonStyle}" Command="{Binding ApplyCommand}"/>-->

    </Grid>
</UserControl>

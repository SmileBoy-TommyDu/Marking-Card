<UserControl x:Class="DrSoft.MarkCard.UI.Views.ReverseEngravingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DrSoft.MarkCard.UI.Views"  
             xmlns:uc="clr-namespace:DrSoft.MarkCard.CommonUI.UserControls;assembly=DrSoft.MarkCard.CommonUI"
             mc:Ignorable="d" 
             Background="#F5F5F5"
             d:DesignHeight="200" d:DesignWidth="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <CheckBox Content="反相雕刻" IsChecked="{Binding Model.Enabled}" Style="{StaticResource DrCheckBoxStyle}"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="上" VerticalAlignment="Center" Margin="4"/>
            <uc:NumericUpDownControl Margin="4,2" Title="" Unit=""  Grid.Row="0" Grid.Column="1" Value="{Binding Model.Top}" Style="{StaticResource MainNumericUpDown}"/>
            <TextBlock Grid.Row="0" Grid.Column="2" Text="左" VerticalAlignment="Center" Margin="16,4,0,0"/>
            <uc:NumericUpDownControl Margin="4,2" Title="" Unit=""  Grid.Row="0" Grid.Column="3" Value="{Binding Model.Left}" Style="{StaticResource MainNumericUpDown}"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="下" VerticalAlignment="Center" Margin="4"/>
            <uc:NumericUpDownControl Margin="4,2" Title="" Unit=""  Grid.Row="1" Grid.Column="1" Value="{Binding Model.Bottom}" Style="{StaticResource MainNumericUpDown}"/>
            <TextBlock Grid.Row="1" Grid.Column="2" Text="右" VerticalAlignment="Center"  Margin="16,4,0,0"/>
            <uc:NumericUpDownControl Margin="4,2" Title="" Unit=""  Grid.Row="1" Grid.Column="3" Value="{Binding Model.Right}" Style="{StaticResource MainNumericUpDown}"/>
        </Grid>
    </Grid>
</UserControl>

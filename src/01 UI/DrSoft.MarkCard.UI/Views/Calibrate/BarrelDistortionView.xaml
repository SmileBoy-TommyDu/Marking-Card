<UserControl x:Class="DrSoft.MarkCard.UI.Views.Calibrate.BarrelDistortionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DrSoft.MarkCard.UI.Views.Calibrate"
             xmlns:uc="clr-namespace:DrSoft.MarkCard.CommonUI.UserControls;assembly=DrSoft.MarkCard.CommonUI"
             mc:Ignorable="d" 
             FontSize="14"
             Foreground="#2a2c2b">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border BorderBrush="#E0E0E0" BorderThickness="1" ></Border>

        <!-- 顶部控制栏 -->
        <Grid Grid.Row="1" Margin="0,4,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="120"/>
        
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="校正幅面" VerticalAlignment="Center" Margin="0,0,0,0"/>
            <uc:NumericUpDownControl Grid.Column="1" Width="120" Value="{Binding CalibrationArea}" Unit="mm"/>

           
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,8">
            <Button Content="1 生成图形" Width="80"  Margin="0,0,10,0" Style="{StaticResource ActionButtonStyle}" Command="{Binding CreateGraphicsCommand}"/>
            <Button Content="2 标刻图形" Width="80" Margin="0,0,10,0" Command="{Binding MarkGraphicsCommand}" Style="{StaticResource SecondActionButtonStyle}"/>
            <Button Content="3 执行校正" Width="80"  Margin="0,0,10,0" Command="{Binding ExecuteAndSaveCommand}" Style="{StaticResource SecondActionButtonStyle}"/>
          
        </StackPanel>

        <!-- 中间内容区域 -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧: 3x3网格预览图 -->
            <Border Grid.Column="0" Background="White" BorderBrush="#CCCCCC" BorderThickness="1" Margin="0,0,10,0">
                <Viewbox Stretch="Uniform">
                    <Canvas Width="250" Height="250" Margin="20">
                        <!-- 3x3 网格线 -->
                        <!-- 垂直线 -->
                        <Line X1="0" Y1="0" X2="0" Y2="250" Stroke="Gray" StrokeThickness="1.5"/>
                        <Line X1="125" Y1="0" X2="125" Y2="250" Stroke="Gray" StrokeThickness="1.5"/>
                        <Line X1="250" Y1="0" X2="250" Y2="250" Stroke="Gray" StrokeThickness="1.5"/>
                        
                        <!-- 水平线 -->
                        <Line X1="0" Y1="0" X2="250" Y2="0" Stroke="Gray" StrokeThickness="1.5"/>
                        <Line X1="0" Y1="125" X2="250" Y2="125" Stroke="Gray" StrokeThickness="1.5"/>
                        <Line X1="0" Y1="250" X2="250" Y2="250" Stroke="Gray" StrokeThickness="1.5"/>
                    
                        <!-- X标识 -->
                        <!-- X+ 标注 -->
                        <TextBlock Canvas.Left="220" Canvas.Top="130" Text="X+" FontSize="16" FontWeight="Bold" Foreground="Black"/>

                        <!-- Y+ 标注 -->
                        <TextBlock Canvas.Left="135" Canvas.Top="6" Text="Y+" FontSize="16" FontWeight="Bold" Foreground="Black"/>

                    </Canvas>
                </Viewbox>
            </Border>

            <!-- 右侧: 宽度输入 -->
            <StackPanel Grid.Column="1" Margin="0,12,0,0" VerticalAlignment="Top">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="宽3" Margin="0,0,0,0" Width="30" VerticalAlignment="Center"/>
                    <uc:NumericUpDownControl Value="{Binding Width3}" Width="90" Unit="mm"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0 90 0 0">
                    <TextBlock Text="宽2" Margin="0,0,0,0" Width="30" VerticalAlignment="Center"/>
                    <uc:NumericUpDownControl Value="{Binding Width2}" Width="90" Unit="mm"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0 90 0 0">
                    <TextBlock Text="宽1" Margin="0,0,0,0" Width="30" VerticalAlignment="Center"/>
                    <uc:NumericUpDownControl Value="{Binding Width1}" Width="90" Unit="mm"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- 底部: 高度输入 -->
        <StackPanel Grid.Row="4" Margin="0,15,0,10" Orientation="Horizontal">


            <StackPanel  Orientation="Vertical">
                <TextBlock Text="高1" Margin="0,0,0,0" Width="30" VerticalAlignment="Center"/>
                <uc:NumericUpDownControl Width="90" Value="{Binding Height1}" Unit="mm"/>
            </StackPanel>

            <StackPanel Orientation="Vertical" Margin="20 0 0 0">
                <TextBlock Text="高2" Margin="0,0,0,0" Width="30" VerticalAlignment="Center"/>
                <uc:NumericUpDownControl Width="90" Value="{Binding Height2}" Unit="mm"/>
            </StackPanel>

            <StackPanel Orientation="Vertical" Margin="20 0 0 0">
                <TextBlock Text="高3" Margin="0,0,0,0" Width="30" VerticalAlignment="Center"/>
                <uc:NumericUpDownControl Width="90" Value="{Binding Height3}" Unit="mm"/>
            </StackPanel>
        </StackPanel>

      
    </Grid>
</UserControl>

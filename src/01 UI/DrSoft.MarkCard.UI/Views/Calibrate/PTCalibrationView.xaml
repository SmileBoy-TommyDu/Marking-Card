<UserControl x:Class="DrSoft.MarkCard.UI.Views.Calibrate.PTCalibrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DrSoft.MarkCard.UI.Views.Calibrate"
             xmlns:uc="clr-namespace:DrSoft.MarkCard.CommonUI.UserControls;assembly=DrSoft.MarkCard.CommonUI"
             mc:Ignorable="d" 
             Background="#E0E0E0">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- PT校正参数设置 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="PT校正参数" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="35"/>
                    <RowDefinition Height="35"/>
                    <RowDefinition Height="35"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="PT参数1" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <uc:NumericInputControl Grid.Row="0" Grid.Column="1" Value="{Binding PTParam1}" Unit="mm"/>

                <TextBlock Grid.Row="1" Text="PT参数2" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <uc:NumericInputControl Grid.Row="1" Grid.Column="1" Value="{Binding PTParam2}" Unit="mm"/>

                <TextBlock Grid.Row="2" Text="PT参数3" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <uc:NumericInputControl Grid.Row="2" Grid.Column="1" Value="{Binding PTParam3}" Unit="mm"/>
            </Grid>

            <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                <Button Content="空走" Width="80" Height="30" Margin="0,0,10,0" Command="{Binding DryRunCommand}"/>
                <Button Content="标刻图形" Width="80" Height="30" Margin="0,0,10,0" Command="{Binding MarkGraphicsCommand}"/>
                <Button Content="应用" Width="80" Height="30" Command="{Binding ApplyCommand}"/>
            </StackPanel>
        </StackPanel>

        <!-- PT校正数据显示 -->
        <Border Grid.Row="1" Background="White" BorderBrush="#CCCCCC" BorderThickness="1">
            <TextBlock Text="PT校正数据将在此处显示" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#999999"/>
        </Border>

        <!-- 底部按钮 -->
        <StackPanel Grid.Row="2" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="停止加工" Width="80" Height="30" Command="{Binding StopProcessCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>

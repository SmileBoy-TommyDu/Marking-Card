<UserControl x:Class="DrSoft.MarkCard.UI.Views.Config.LaserConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:uc="clr-namespace:DrSoft.MarkCard.CommonUI.UserControls;assembly=DrSoft.MarkCard.CommonUI"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="600"
             >
    <Grid Margin="8 8 8 8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="{StaticResource MyBorderGradient}" Height="20">
            <TextBlock Grid.Row="0" Text="激光器" Margin="4 0 0 0" VerticalAlignment="Center" FontSize="14" Foreground="#ffffff"/>
        </Border>
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <!--<TextBlock Text="基础设置" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,10"/>-->

            <Grid Margin="0,4,0,0">
                <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="2"/>
                        <ColumnDefinition Width="128"/>
                        <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="打标卡类型" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0 0 8 0"/>
                <ComboBox Grid.Column="1" Style="{StaticResource DrComboxStyle}"
                          ItemsSource="{Binding MarkCardTypes}"
                          SelectedItem="{Binding SelectedMarkCardType}"
                          VerticalContentAlignment="Center" Width="120" HorizontalAlignment="Left"/>

                <TextBlock Grid.Column="3" Text="打标卡序号" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0 0 8 0"/>
                <ComboBox Grid.Column="4" Style="{StaticResource DrComboxStyle}"
                          ItemsSource="{Binding CardNos}"
                          SelectedItem="{Binding SelectedCardNo}"
                          VerticalContentAlignment="Center" Width="120" HorizontalAlignment="Left"/>
            </Grid>

            <Grid Margin="0,4,0,0">
                <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="2"/>
                    <ColumnDefinition Width="128"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="激光型号" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0 0 8 0"/>
                <ComboBox Grid.Column="1" Style="{StaticResource DrComboxStyle}"
                          ItemsSource="{Binding LaserModels}"
                          SelectedItem="{Binding SelectedLaserModel}"
                          VerticalContentAlignment="Center" Width="120" HorizontalAlignment="Left"/>

                <TextBlock Grid.Column="3" Text="激光器类型" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0 0 8 0"/>
                <ComboBox Grid.Column="4" Style="{StaticResource DrComboxStyle}"
                          ItemsSource="{Binding LaserTypes}"
                          SelectedItem="{Binding SelectedLaserType}"
                          VerticalContentAlignment="Center" Width="120" HorizontalAlignment="Left"/>
            </Grid>

            <Grid Margin="0,4,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="130"/>
                    <ColumnDefinition Width="2"/>
                    <ColumnDefinition Width="128"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="激光理论功率" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0 0 8 0"/>
                <uc:NumericUpDownControl Grid.Column="1" Value="{Binding ConfiguredTheoreticalPower}" MinValue="0" MaxValue="1000000" Unit="W" Width="120" HorizontalAlignment="Left"/>

                <TextBlock Grid.Column="3" Text="0到满功率所需时间" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0 0 8 0"/>
                <uc:NumericUpDownControl Grid.Column="4" Value="{Binding ConfiguredPowerRampUpTime}" MinValue="0" MaxValue="1000000" Unit="ms" Width="120" HorizontalAlignment="Left"/>
            </Grid>

            <Grid Margin="0,4,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="130"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="稳定延时时间" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0 0 8 0"/>
                <uc:NumericUpDownControl Grid.Column="1" MinValue="0" MaxValue="1000000" Value="{Binding ConfiguredPowerStabilizationDelay}" Unit="ms" Width="120" HorizontalAlignment="Left"/>
            </Grid>
        </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>

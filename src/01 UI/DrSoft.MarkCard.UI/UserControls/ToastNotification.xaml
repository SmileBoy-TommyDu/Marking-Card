<UserControl x:Class="DrSoft.MarkCard.UI.UserControls.ToastNotification"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="60" d:DesignWidth="300"
             MinWidth="604"
             MinHeight="40"
             x:Name="Root">
    <UserControl.Resources>
        <!-- Info 颜色 -->
        <SolidColorBrush x:Key="InfoBackgroundColor" Color="#CAEAD2"/>
        <SolidColorBrush x:Key="InfoForegroundColor" Color="#000000"/>
        <SolidColorBrush x:Key="InfoBorderColor" Color="#8EDDA2"/>

        <!-- Warning 颜色 -->
        <SolidColorBrush x:Key="WarningBackgroundColor" Color="#F2E6C2"/>
        <SolidColorBrush x:Key="WarningForegroundColor" Color="#000000"/>
        <SolidColorBrush x:Key="WarningBorderColor" Color="#F4D479"/>

        <!-- Error 颜色 -->
        <SolidColorBrush x:Key="ErrorBackgroundColor" Color="#EFCACA"/>
        <SolidColorBrush x:Key="ErrorForegroundColor" Color="#000000"/>
        <SolidColorBrush x:Key="ErrorBorderColor" Color="#EA8D8D"/>
    </UserControl.Resources>

    <Border x:Name="MainBorder"
            Padding="15,10"
            CornerRadius="4"
            BorderThickness="1">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Visibility" Value="Visible"/>
                <Setter Property="Background" Value="{StaticResource InfoBackgroundColor}"/>
                <Setter Property="BorderBrush" Value="{StaticResource InfoBorderColor}"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsVisible}" Value="False">
                        <Setter Property="Visibility" Value="Collapsed"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding ToastType}" Value="Info">
                        <Setter Property="Background" Value="{StaticResource InfoBackgroundColor}"/>
                        <Setter Property="BorderBrush" Value="{StaticResource InfoBorderColor}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding ToastType}" Value="Error">
                        <Setter Property="Background" Value="{StaticResource ErrorBackgroundColor}"/>
                        <Setter Property="BorderBrush" Value="{StaticResource ErrorBorderColor}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding ToastType}" Value="Warning">
                        <Setter Property="Background" Value="{StaticResource WarningBackgroundColor}"/>
                        <Setter Property="BorderBrush" Value="{StaticResource WarningBorderColor}"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.3" Color="Black"/>
        </Border.Effect>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- 图标 -->
            <Image Grid.Column="0"
                   x:Name="IconImage"
                   Width="24"
                   Height="24"
                   VerticalAlignment="Center"
                   Margin="0,0,10,0"
                   Stretch="Uniform">
                <Image.Style>
                    <Style TargetType="Image">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ToastType}" Value="Info">
                                <Setter Property="Source" Value="/Resource/image/Toast/info.png"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ToastType}" Value="Error">
                                <Setter Property="Source" Value="/Resource/image/Toast/error.png"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ToastType}" Value="Warning">
                                <Setter Property="Source" Value="/Resource/image/Toast/warning.png"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Image.Style>
            </Image>

            <!-- 消息文本 -->
            <TextBlock Grid.Column="1"
                       Text="{Binding Message}"
                       VerticalAlignment="Center"
                       TextWrapping="Wrap">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="Foreground" Value="{StaticResource InfoForegroundColor}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ToastType}" Value="Info">
                                <Setter Property="Foreground" Value="{StaticResource InfoForegroundColor}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ToastType}" Value="Error">
                                <Setter Property="Foreground" Value="{StaticResource ErrorForegroundColor}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ToastType}" Value="Warning">
                                <Setter Property="Foreground" Value="{StaticResource WarningForegroundColor}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <!-- 关闭按钮 -->
            <Button Grid.Column="2"
                    Content="✕"
                    Command="{Binding CloseCommand}"
                    Cursor="Hand"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Padding="5"
                    Margin="10,0,0,0">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Foreground" Value="{StaticResource InfoForegroundColor}"/>
                        <Setter Property="FontSize" Value="16"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ToastType}" Value="Info">
                                <Setter Property="Foreground" Value="{StaticResource InfoForegroundColor}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ToastType}" Value="Error">
                                <Setter Property="Foreground" Value="{StaticResource ErrorForegroundColor}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ToastType}" Value="Warning">
                                <Setter Property="Foreground" Value="{StaticResource WarningForegroundColor}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</UserControl>

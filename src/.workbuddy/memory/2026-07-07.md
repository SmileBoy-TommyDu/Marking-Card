# 2026-07-07 工作日志

## RTC6打标卡接口清单整理
- 分析了 `04 Infrastructure/DrSoft.MarkCard.RTC` 模块的完整实现逻辑
- 涉及文件：RTC6Adapter.cs、RTC6Wrap.cs、RTC6ADDONWrap.cs、CommandProcessors/*.cs、IMarkCardAdapter.cs
- 生成了7个Sheet的Excel接口清单：
  1. 概览 - 11个模块分类汇总
  2. 适配器接口(IMarkCardAdapter) - 52个业务接口（含3个未实现）
  3. RTC6底层API(RTC6Wrap) - 55个RTC6 DLL函数
  4. 打标命令处理器 - 13个CommandProcessor
  5. RTC6ADDON短向量API - 12个ShortVector函数
  6. 校正库接口(CalLib) - 4个slcl校正函数
  7. 工艺参数与数据模型 - 36个字段/枚举/常量
- 关键发现：
  - factor 为坐标缩放因子(mm→bit)，由 get_head_para 获取
  - 激光延时/脉宽参数传底层需 ×64（RTC6时间基准1/64μs）
  - 振镜延时参数传底层需 /10
  - GetScannerAcc/SetScannerAcc/GetScannerConnect 未实现(NotImplementedException)
  - 虚线打标基于 RTC6ADDON ShortVector 功能，需 activateShortVectors 激活
- 输出文件1：`04 Infrastructure/DrSoft.MarkCard.RTC/RTC6打标卡接口清单.xlsx`（基于完整模块分析，9个Sheet）
- 输出文件2：`04 Infrastructure/DrSoft.MarkCard.RTC/IMarkCardAdapter接口清单.xlsx`（以IMarkCardAdapter为主线，6个Sheet，52个接口按功能分类）

## RTC6打标卡接口清单 - 详细版更新
- 用户要求在Excel中列出更详细的接口信息
- 重新深入分析了全部源码，提取完整方法签名、参数类型、返回值、实现逻辑
- 重新生成9个Sheet的详细版Excel（35KB）：
  1. 概览 - 模块汇总 + 10条关键实现备注
  2. 适配器接口(IMarkCardAdapter) - 48个方法，含完整签名/参数说明/实现状态/底层API调用链
  3. RTC6底层API(RTC6Wrap) - 67个实际使用的DLL函数，含委托签名/参数/返回值/调用位置
  4. 打标命令处理器 - 13个Processor，含处理逻辑/底层API/参数换算公式
  5. RTC6ADDON短向量API - 15个ShortVector函数
  6. 校正库接口(CalLib) - 4个slcl函数（32/64位双签名）
  7. 工艺参数与数据模型 - ProcessParam(14字段) + Context(10字段) + MarkCommandType(13枚举)
  8. 错误码对照表 - 67个MarkErrorCode枚举值及触发场景
  9. 常量与配置 - 34个内部常量/IO命令码/校正选项枚举/DLL路径
- 新增内容：完整方法签名、参数换算公式、调用链追踪、错误码触发场景

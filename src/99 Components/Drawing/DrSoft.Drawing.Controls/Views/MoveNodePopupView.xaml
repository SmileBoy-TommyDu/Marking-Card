<UserControl x:Class="DrSoft.Drawing.Controls.Views.MoveNodePopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:DrSoft.Drawing.Controls.ViewModels" 
             xmlns:uc1="clr-namespace:DrSoft.MarkCard.CommonUI.UserControls;assembly=DrSoft.MarkCard.CommonUI"
             d:DataContext="{d:DesignInstance Type=viewmodels:MoveNodePopupViewModel}"
             mc:Ignorable="d" 
             Height="120" Width="340">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--<TextBlock Grid.Row="0" Text="节点坐标移动" FontSize="14" FontWeight="SemiBold" 
                   Foreground="#FF5B00"/>-->

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="110"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Column="1" Grid.Row="0" Text="X" VerticalAlignment="Center"  TextAlignment="Center" FontWeight="SemiBold"/>
            <uc1:NumericUpDownControl Grid.Column="2" Grid.Row="0"  Unit="mm" MinValue="-1000" MaxValue="1000" Width="100"  Value="{Binding InputX, StringFormat=F2}" />
            <TextBlock Grid.Column="1" Grid.Row="1" Text="Y" VerticalAlignment="Center"  TextAlignment="Center" FontWeight="SemiBold"/>
            <uc1:NumericUpDownControl Grid.Column="2" Grid.Row="1"  Unit="mm" MinValue="-1000" MaxValue="1000" Width="100" Value="{Binding InputY, StringFormat=F2}"/>
        </Grid>
    </Grid>
</UserControl>